# Environment Config

# store your secrets and config variables in here
# only invited collaborators will be able to see your .env values

# reference these in your code with process.env.KEYNAME

#mongodb+srv://<username>:<password>@cluster0-m2qcm.mongodb.net/test?retryWrites=true&w=majority

USERNAME=
PASSWORD=
HOST=
DATABASE=
NODE_ENV=

# note: .env is a shell file so there can't be spaces around =
